apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dau-dx-api-frontend-redirect
spec:
  parentRefs:
    - name: shared-gw
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - dxapi.donga.ac.kr
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 302
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dau-dx-api-frontend
spec:
  parentRefs:
    - name: shared-gw
      namespace: nginx-gateway
      sectionName: https
  hostnames:
    - dxapi.donga.ac.kr
  rules:
    - backendRefs:
        - name: dau-dx-api-frontend
          port: 80
